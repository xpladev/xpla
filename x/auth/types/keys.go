package types

import (
	"cosmossdk.io/collections"
)

var (
	// SliceAddressStoreKeyPrefix prefix for slice_address-by-address store
	SliceAddressStoreKeyPrefix = collections.NewPrefix("sliceAddress")
)
